# 主机系统 - 添加主机完整教程

> 本文档详细说明如何在主机系统中添加EasyPanel和MNBT主机

---

## 📋 前置准备

### 1. 完成系统部署

确保主机系统已正确部署并可以访问：
- 前台：http://your-domain.com
- 后台：http://your-domain.com/admin
- 默认账号：admin / 123456

### 2. 获取主机平台信息

#### EasyPanel
1. 登录EasyPanel后台
2. 进入「系统设置」→「API配置」
3. 获取API秘钥（对接秘钥）

#### MNBT
1. 登录MNBT后台
2. 进入「宝塔面板列表」
3. 获取宝塔编号（账号）和调用秘钥（密码）
4. 进入「系统设置」→「API设置」
5. 获取API秘钥（安全码）

---

## 🔌 添加EasyPanel主机

### 步骤1：进入插件管理

1. 登录主机系统后台
2. 左侧菜单 → 「主机管理」→ 「主机插件」
3. 找到「EasyPanel」插件，点击「配置」

### 步骤2：填写配置信息

| 配置项 | 填写说明 | 示例 |
|--------|----------|------|
| **主机地址** | EasyPanel的域名或IP，**不要**带http/https | `panel.example.com` 或 `192.168.1.100` |
| **安全码** | EasyPanel的API对接秘钥 | `abcdef123456` |
| **SSL** | 如果EasyPanel使用HTTPS，勾选此项 | 勾选 ✓ |
| **端口** | 如果端口不是80，填写端口号 | `8080` |

### 步骤3：测试连接

1. 填写完成后，点击「测试连接」
2. 如果提示「连接成功」，点击「保存配置」
3. 如果提示「连接失败」，检查：
   - 主机地址是否正确
   - API秘钥是否正确
   - 防火墙是否允许访问
   - 网络是否连通

### 步骤4：添加主机套餐

1. 左侧菜单 → 「主机管理」→ 「主机套餐」
2. 点击「添加套餐」
3. 填写套餐信息：
   - **套餐名称**：如「入门版」「专业版」
   - **主机类型**：选择EasyPanel
   - **存储空间**：如 1GB、5GB
   - **带宽限制**：如 100MB/s
   - **数据库数量**：如 2个
   - **价格**：设置月付、年付价格
4. 点击「保存」

### 步骤5：测试购买流程

1. 前台访问网站
2. 选择刚创建的套餐
3. 完成支付（测试支付）
4. 检查是否自动创建主机

---

## 🔌 添加MNBT主机

### 步骤1：进入插件管理

1. 登录主机系统后台
2. 左侧菜单 → 「主机管理」→ 「主机插件」
3. 找到「MNBT」插件，点击「配置」

### 步骤2：填写配置信息

| 配置项 | 填写说明 | 示例 |
|--------|----------|------|
| **主机地址** | MNBT的域名或IP，**不要**带http/https | `mnbt.example.com` 或 `192.168.1.100` |
| **安全码** | MNBT后台系统API秘钥 | `abcdef123456` |
| **账号** | MNBT宝塔列表的宝塔编号 | `1` |
| **密码** | MNBT宝塔列表的调用秘钥 | `xyz789` |
| **SSL** | 如果MNBT使用HTTPS，勾选此项 | 勾选 ✓ |
| **端口** | 如果端口不是80，填写端口号 | `8080` |

### 步骤3：测试连接

1. 填写完成后，点击「测试连接」
2. 如果提示「连接成功」，点击「保存配置」
3. 如果提示「连接失败」，检查：
   - 主机地址是否正确
   - API秘钥、账号、密码是否正确
   - 防火墙是否允许访问
   - 网络是否连通

### 步骤4：同步宝塔列表

1. 左侧菜单 → 「主机管理」→ 「同步主机」
2. 点击「同步宝塔列表」
3. 等待同步完成，查看已同步的宝塔面板

### 步骤5：添加主机套餐

1. 左侧菜单 → 「主机管理」→ 「主机套餐」
2. 点击「添加套餐」
3. 填写套餐信息：
   - **套餐名称**：如「入门版」「专业版」
   - **主机类型**：选择MNBT
   - **宝塔编号**：选择刚才同步的宝塔
   - **存储空间**：如 1GB、5GB
   - **带宽限制**：如 100MB/s
   - **数据库数量**：如 2个
   - **价格**：设置月付、年付价格
4. 点击「保存」

### 步骤6：测试购买流程

1. 前台访问网站
2. 选择刚创建的套餐
3. 完成支付（测试支付）
4. 检查是否在宝塔上自动创建主机

---

## ⚠️ 常见问题排查

### 连接失败

| 问题 | 解决方案 |
|------|----------|
| 无法访问主机地址 | 检查网络连通性，ping主机地址 |
| API秘钥错误 | 重新获取API秘钥，确认复制正确 |
| 防火墙拦截 | 在主机防火墙开放端口 |
| SSL证书问题 | 取消勾选SSL或使用自签证书 |

### 创建主机失败

| 问题 | 解决方案 |
|------|----------|
| 宝塔API返回错误 | 检查宝塔API权限，确认开放了主机管理权限 |
| 数据库创建失败 | 检查数据库配置，确认数据库用户有权限 |
| 套餐信息不完整 | 检查套餐配置，确保必填项已填写 |

### 支付问题

| 问题 | 解决方案 |
|------|----------|
| 支付成功但主机未创建 | 检查支付回调日志，确认回调地址正确 |
| 支付回调失败 | 检查回调地址是否可达，确认接口正常 |
| 金额不符 | 检查支付金额配置，确认与套餐价格一致 |

---

## 📞 技术支持

- 原作者QQ：2150811531
- QQ群：905412821
